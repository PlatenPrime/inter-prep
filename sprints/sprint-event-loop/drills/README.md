# Drills — Sprint Event Loop

38 puzzle folders. For each:

1. Read `puzzle.md`
2. Write prediction to `answer.txt`
3. `node check.mjs`
4. Optional: `node snippet.mjs` then compare with `answer.md`

Run all checks: `npm run sprint-event-loop:drills`
