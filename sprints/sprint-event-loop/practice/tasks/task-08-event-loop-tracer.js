/**
 * Event loop tracer
 * Map schedule kinds to human-readable phase labels.
 */
/**
 * @typedef {'sync'|'micro'|'macro'|'node-nextTick'} TaskKind
 * @param {TaskKind} kind
 * @returns {string}
 */

export function tracePhase(kind) {
  throw new Error('Not implemented');
}
