/**
 * Task 04 — Parse simplified git log --oneline
 *
 * Each line: "<shortSha> <message>"
 * Return array of { sha, message } objects in order.
 * Skip empty lines. Lines without space after sha are invalid — skip them.
 *
 * @param {string} logOutput
 * @returns {{ sha: string; message: string }[]}
 */

// TODO: implement parseGitLog(logOutput)

export function parseGitLog(logOutput) {
  throw new Error('Not implemented');
}
