/**
 * Task 04 — Resolve nested design token path
 *
 * @param {object} tokens - nested token tree
 * @param {string} path - dot-separated path e.g. "colors.blue.500"
 * @returns {string | number | undefined}
 */

// TODO: implement resolveToken(tokens, path)

export function resolveToken(tokens, path) {
  throw new Error('Not implemented');
}
