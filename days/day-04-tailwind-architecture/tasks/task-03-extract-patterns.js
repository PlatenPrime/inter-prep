/**
 * Task 03 — Extract repeated class patterns across components
 *
 * @param {{ classes: string }[]} components
 * @param {number} minCount - minimum occurrences (default 2)
 * @returns {{ pattern: string, count: number }[]} sorted by count desc, then pattern asc
 */

// TODO: implement extractRepeatedPatterns(components, minCount)

export function extractRepeatedPatterns(components, minCount = 2) {
  throw new Error('Not implemented');
}
