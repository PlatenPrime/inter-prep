/**
 * Task 02 — Merge utility class strings (last wins per CSS property)
 *
 * @param {string[]} classStrings - applied in order (later overrides)
 * @returns {object} merged style object
 */

// TODO: implement mergeUtilities(classStrings)

export function mergeUtilities(classStrings) {
  throw new Error('Not implemented');
}
