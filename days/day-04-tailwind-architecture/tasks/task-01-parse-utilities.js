/**
 * Task 01 — Parse Tailwind-like utility classes
 *
 * Map known utilities to style objects (see solutions UTILITY_MAP for keys).
 * Unknown classes are ignored.
 *
 * @param {string} classString
 * @returns {object}
 */

// TODO: implement parseUtilities(classString)

export function parseUtilities(classString) {
  throw new Error('Not implemented');
}
