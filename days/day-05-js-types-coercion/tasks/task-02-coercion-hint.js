/**
 * Task 02 — Type tags and numeric coercion
 *
 * coercionHint(value) → 'null' | 'undefined' | 'boolean' | 'string' | 'number' | 'nan' | 'bigint' | 'symbol' | 'function' | 'array' | 'date' | 'object'
 * coerceToNumber(value) → number per ToNumber rules (simplified)
 */

// TODO: implement coercionHint(value) and coerceToNumber(value)

export function coercionHint(value) {
  throw new Error('Not implemented');
}

export function coerceToNumber(value) {
  throw new Error('Not implemented');
}
