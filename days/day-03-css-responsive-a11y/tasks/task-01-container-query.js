/**
 * Task 01 — Container query style matching
 *
 * rules: { minWidth?, maxWidth?, styles: object }[]
 * Return merged styles from all rules matching containerWidth (in order).
 *
 * @param {number} containerWidth
 * @param {object[]} rules
 * @returns {object}
 */

// TODO: implement matchContainerStyles(containerWidth, rules)

export function matchContainerStyles(containerWidth, rules) {
  throw new Error('Not implemented');
}
