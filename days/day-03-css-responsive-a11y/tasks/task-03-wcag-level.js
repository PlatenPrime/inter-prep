/**
 * Task 03 — WCAG contrast level
 *
 * Return 'AAA' | 'AA' | 'fail' for given contrast ratio.
 * - Normal text: AA >= 4.5, AAA >= 7
 * - Large text: AA >= 3, AAA >= 4.5
 * - UI components / graphical objects: AA >= 3 only (no AAA tier)
 *
 * @param {number} ratio
 * @param {{ isLargeText?: boolean, isUiComponent?: boolean }} options
 */

// TODO: implement wcagContrastLevel(ratio, options)

export function wcagContrastLevel(ratio, options) {
  throw new Error('Not implemented');
}
