/**
 * Task 02 — WCAG contrast ratio
 *
 * Calculate contrast ratio between two hex colors (#RGB or #RRGGBB).
 * WCAG formula: (L1 + 0.05) / (L2 + 0.05) where L1 is lighter luminance.
 *
 * @param {string} hex1
 * @param {string} hex2
 * @returns {number} ratio rounded to 2 decimals
 */

// TODO: implement contrastRatio(hex1, hex2)

export function contrastRatio(hex1, hex2) {
  throw new Error('Not implemented');
}
