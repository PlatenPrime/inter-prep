/**
 * Task 04 — clamp() evaluation (simplified)
 *
 * evaluateClamp(min, preferredPx, preferredVw, max, viewportWidth)
 * preferred = preferredPx + preferredVw * viewportWidth / 100
 * result = clamp(min, preferred, max)
 */

// TODO: implement evaluateClamp(min, preferredPx, preferredVw, max, viewportWidth)

export function evaluateClamp(min, preferredPx, preferredVw, max, viewportWidth) {
  throw new Error('Not implemented');
}
