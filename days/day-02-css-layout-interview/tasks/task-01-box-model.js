/**
 * Task 01 — Box model outer dimensions
 *
 * Compute margin-edge (outer) width and height.
 * - content-box: width/height are content dimensions
 * - border-box: width/height include padding + border (not margin)
 *
 * padding, border, margin: number (all sides) or { top, right, bottom, left }
 *
 * @param {object} opts
 * @returns {{ outerWidth: number, outerHeight: number }}
 */

// TODO: implement computeOuterSize(opts)

export function computeOuterSize(opts) {
  throw new Error('Not implemented');
}
