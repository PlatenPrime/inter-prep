/**
 * Task 03 — Flex grow distribution
 *
 * Distribute positive free space proportionally by flexGrow.
 * containerWidth is inner width (after container padding).
 * gap is px between items.
 * items: { flexGrow, flexBasis, minWidth? }[]
 *
 * @returns {number[]} final item widths
 */

// TODO: implement distributeFlexWidths(containerWidth, gap, items)

export function distributeFlexWidths(containerWidth, gap, items) {
  throw new Error('Not implemented');
}
