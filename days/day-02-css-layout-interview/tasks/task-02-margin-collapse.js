/**
 * Task 02 — Vertical margin collapse
 *
 * Given consecutive vertical margins between stacked blocks (in px),
 * return the single collapsed margin (CSS simplified rules):
 * - both >= 0 → max
 * - both < 0 → min
 * - mixed signs → sum
 *
 * @param {number[]} margins
 * @returns {number}
 */

// TODO: implement collapseMargins(margins)

export function collapseMargins(margins) {
  throw new Error('Not implemented');
}
