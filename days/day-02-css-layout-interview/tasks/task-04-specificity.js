/**
 * Task 04 — CSS specificity tuple
 *
 * Return [ids, classes, elements] for a single simplified selector.
 * - #id → ids
 * - .class, [attr], :pseudo-class → classes
 * - element, ::pseudo-element → elements
 *
 * @param {string} selector
 * @returns {[number, number, number]}
 */

// TODO: implement specificity(selector)

export function specificity(selector) {
  throw new Error('Not implemented');
}
